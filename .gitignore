# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environments
.env
.env.local
.env.*.local
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class

# Node
node_modules/
dist/
build/
.next/
out/
coverage/

# Data
# Bulk corpus files (raw/normalized MusicXML) live locally; only the
# manifest, rejection log, candidate list, and report are committed so the
# corpus is reproducible from those + the pipeline.
/data/
/corpus/raw/
/corpus/normalized/
/corpus/cache/
*.musicxml
*.mxl
*.pdf

# Probe artifacts — large datasets downloaded for one-off feasibility
# checks. Reports and probe scripts are committed; the raw downloads
# (~2 GB each) are not.
/experiments/pdmx_probe/PDMX.csv
/experiments/pdmx_probe/*.tar.gz
/experiments/pdmx_probe/subset_paths/
/experiments/pdmx_probe/candidates.json
/experiments/gaps_probe/*.zip
/experiments/gaps_probe/gaps_v1/
